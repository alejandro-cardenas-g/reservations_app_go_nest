version: '3.8'
services:
  postgres-latest:
    image: postgres:18
    ports:
      - '5434:5432'
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: reservations
    container_name: db_reservations_local
    volumes:
      - ./postgres:/var/lib/postgresql
